# Project pointer (locked)

- FreedomGame lives at `F:\FreedomGame\FreedomGame.uproject` (engine 5.8, Blank/Blueprint/Scalable). OUTSIDE this repo on purpose: repo sits on C: (17 GB free), project weight + spaces-risk stay on F:.
- This repo tracks papers, configs, agents, scripts. Never commit the UE project here.
- Project-local wiring (done by manager, in project files): ModelContextProtocol + EditorToolset plugins enabled; MCP autostart 8000; Interchange FBX flag off (manual-FBX path).
- Live verification happens per session (port + registries), recorded in probe/build reports.
