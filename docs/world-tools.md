# Track 2 — World tools (researched + on-disk verified, live use at execution)

## Verdicts
- Bundled, present on disk (verified 2026-09-21): PCGToolset, Water, Landmass, Landscape + Splines. Free forever, enable-in-Plugins at execution. PCG Framework is production in 5.8 (needs PCG Framework plugin + Geometry Script Interop for mesh sampling; supports Partition + HLOD + templates). PCG Biome Core/Sample = Experimental → DISCARDED for ship path.
- Refs: Geofabrik Syria OSM (free, ODbL — patterns only, fictionalize names, credit line) + SRTM/Copernicus 30m (free base; Blender heights demoted to hero-fix only per re-research).
- Scatter/surfaces: Poly Haven (CC0) + Fab free rotation (verify Standard per asset). Conditional new Fab aids (check free+version at execution): Road Essentials Modular Roads, Easy Waterscape, Normandy Village + PCG Plants, PCG Tropical Highlands. Spline-tools doc moved — re-check at execution; practice unchanged (splines as PCG data source, never hand-model roads).
- MCP-for-world split (execution): AI-suitable = list/find actors, get/set PCG params + regenerate, single-actor spawn probe, audit screenshots. Human-side = spline edits, landscape commit, bulk scatter, Water/Landmass setup, Partition/HLOD submit, deletes.
- NOT free for shipping: Gaea Community (non-commercial), World Creator Community (export disabled), post-Dec-2024 Megascans (paid unless entitled), Sketchfab NC/Editorial, ANY paid road/terrain plugin (Splines+PCG+Fab cover it).

## Minimal chain for slice 1 (5 links, locked)
OSM+SRTM sketch → Blender height → Landscape+Partition import → PCG scatter (Poly Haven + free kit) → bundled Spline roads.
NOT-now: paid terrain tools, road plugins, traffic/AI, real-place names, full-Syria import.

## Sensitivity rule (locked)
Road patterns only. No real family homes, no real victim places, no real names on map. Fictional district always.
