# Docs — papers, not code

Simple rule: if it's an idea, plan, or story, it lives here as a short paper.

- `vision.md` — what game is, what feeling, what scope (one district first).
- `foundation.md` — tech rules so we can grow without rewrite (approved before building).
- `slice-01.md` — first playable test definition (deferred, too early now).
- `team.md` — empty chairs + what each owns (paper only for now).

No Unreal files here. No heavy stuff.
